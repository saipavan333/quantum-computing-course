/* NOT DEPLOYED — placeholder, safe to delete.

   The live AI-assistant proxy is a SINGLE shared Cloudflare Worker used by all of
   these courses. Its source of truth is `worker/assistant-proxy.js` (mirrored in
   this folder), and it is already deployed at:

     https://gd-assistant-proxy.uekpavanharish.workers.dev

   To change the deployed Worker, edit assistant-proxy.js and redeploy that one
   Worker in the Cloudflare dashboard — do not create a second Worker from this
   file. (This mount can't delete files from the sandbox; you can remove this
   stub from your file explorer if you want a tidy repo.) */
